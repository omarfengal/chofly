# CHOFLY Assets

Place image assets here. Required images:
- logo.png        — App logo (used on splash/onboarding)
- logo_white.png  — White version for dark backgrounds

These directories were created to satisfy pubspec.yaml asset declarations.
Flutter requires declared asset directories to exist at build time.
